{
  "name": "@helpdesk/workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:integration": "vitest run src/bootstrap/worker-app.test.ts"
  },
  "dependencies": {
    "@helpdesk/contracts": "workspace:*",
    "@helpdesk/database": "workspace:*",
    "bullmq": "^5.58.5",
    "ioredis": "^5.7.0",
    "zod": "^4.1.5"
  }
}
