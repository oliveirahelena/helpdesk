{
  "name": "@helpdesk/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:integration": "vitest run src/app/routes/health.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@helpdesk/contracts": "workspace:*",
    "@helpdesk/database": "workspace:*",
    "fastify": "^5.6.0",
    "fastify-plugin": "^5.0.1",
    "zod": "^4.1.5"
  }
}
